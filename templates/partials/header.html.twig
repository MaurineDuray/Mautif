<nav class="mainMenu">
    

    <div id="burgerMenu">
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
    </div>
  
    
    
    {# <a href="" class="" style="width: 40px; height: 40px"><img src="{{asset('build/images/zoom.png')}}" style="width: 50px; height: 50px" alt=""></a> #}
    <div id="account">
        <div class="icons" id="search">
            <div id="loupe">
                <i class="icon fa-solid fa-magnifying-glass fa-2x"></i>
            </div>
            <form action="">
                <input type="research" id="research" name="research" placeholder="Rechercher un motif">
            </form>
            
        </div>
        <div class="icons">
            <div id="user">
                <i class="icon fas fa-user fa-2x" ></i>
            </div>  
        </div>
    </div>
</nav>

<nav id="slideMenu">

        <ul>
            <li><a href={{path("homepage")}}#home>Accueil</a></li>
            <li><a href={{path("homepage")}}#about>Comment ça marche&nbsp?</a></li>
            <li><a href={{path("patterns_index")}}>Patchwork</a></li>
        </ul>
</nav>

<div id="accountBlock">
    {% if app.user %}
        <a href="{{path('pattern_add')}}">Publier un motif</a>
        <a href="{{path('user_profile',{'slug': app.user.slug})}}">Mon compte</a>
        <a href="{{path('user_patterns',{'slug': app.user.slug})}}">Mes créations</a>
        {# <a href="">Mes favoris</a> #}
        {% if is_granted('ROLE_ADMIN') %}
             <a href="{{path('admin')}}">ADMINISTRATION</a>
        {% endif %}
        <a href="{{path('account_logout')}}" id="logout"><b>DÉCONNEXION</b></a>
       
    {% else %}
        <a href="{{path('account_login')}}">Connexion</a>
        <a href="{{path('account_register')}}">Créer un compte</a>
    {% endif %}
</div>


{% block javascripts %}
    {{encore_entry_script_tags('menu')}}
{% endblock %}