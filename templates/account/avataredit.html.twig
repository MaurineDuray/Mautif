{% extends "base.html.twig" %}

{% block title %}Modification de l'avatar {% endblock %}
{% block body %}
    <div class="slide" id="connexion">
        <div class="inscriptionContent">
            <h1>Modification de l'avatar</h1>
            {{form_start(myform)}}
                {{form_widget(myform)}}
                <input type="submit" class="btn btn-success" value="Modifier l'avatar"> 
            {{form_end(myform)}}
        </div> 
    </div>
{% endblock %}